/*
slice()

"Give me a copy of a portion of the array."

It never changes the original array.
*/

let colors = ["Red", "Blue", "Green", "Yellow", "Orange"];
console.log(colors);
console.log(colors.slice(1, 3));
//let favorites = colors.slice(1, 3); 
console.log(colors);